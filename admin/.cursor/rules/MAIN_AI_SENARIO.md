خش‌های مختلف هوش مصنوعی

در پروژه، ممکن است تا ۱۰۰ مدل مختلف AI اضافه شود. این مدل‌ها برای بخش‌های زیر استفاده می‌شوند:

تولید محتوا

تولید تصویر

تولید پادکست

چت آنلاین

هیچ‌کدام از خروجی‌ها به‌صورت خودکار در دیتابیس ذخیره نمی‌شوند.

فقط تولید محتوا و تولید پادکست در صورت تمایل کاربر می‌توانند ذخیره شوند (برای وبلاگ، نمونه‌کار یا اپ‌های دیگر).

تولید تصویر نیز در صورت نیاز می‌تواند در مدیای مرکزی ذخیره شود.

🔑 پشتیبانی از دو نوع API Key

در سیستم، دو نوع API وجود دارد:

1) نوع اشتراکی (Shared)

مخصوص سوپر ادمین‌ها

سوپرادمین مشخص می‌کند که فعال باشد یا نباشد

هر مدل AI اگر روی Shared قرار بگیرد، تمام ادمین‌ها می‌توانند از آن استفاده کنند

2) نوع شخصی (Personal)

هر ادمین می‌تواند API اختصاصی خودش را وارد کرده و از آن استفاده کند

اگر ادمین API شخصی تنظیم کرده باشد، همان API باید استفاده شود

اگر API شخصی نداشت یا غیرفعال بود و Shared فعال بود، API Shared استفاده می‌شود

🤖 منابع مدل‌ها

ما چندین سرویس AI داریم که هرکدام ممکن است تا ۴۰۰ مدل پولی و رایگان پشتیبانی کنند، مثل:

OpenRouter

HuggingFace

و سرویس‌های دیگر

برای این سرویس‌ها، در پنل ادمین بخشی ایجاد شده تا بتوانند:

مدل دلخواه هر سرویس را انتخاب کنند

چون مدل‌ها دائماً اضافه/حذف/آپدیت می‌شوند
→ نمی‌توان مدل‌ها را ثابت در بک‌اند یا فرانت‌اند قرار داد

⚠️ مسئلهٔ مهم: تنها یک مدل فعال برای هر بخش

برای هر سرویس و هر بخش از سیستم، فقط یک مدل باید فعال باشد.

مثال:

در بخش چت آنلاین → فقط یک مدل فعال از OpenRouter

در بخش تولید محتوا → فقط یک مدل فعال

در بخش تولید تصویر → فقط یک مدل فعال

و…

❌ فعال بودن چند مدل برای یک بخش (مثلاً ۱۰ مدل از OpenRouter برای چت) باعث تداخل می‌شود و نباید اتفاق بیفتد.

بنابراین:

برای هر بخش، دقیقاً یک مدل فعال می‌تواند وجود داشته باشد.

🔁 انتخاب API Key بر اساس شرایط

سیستم باید طبق اولویت زیر عمل کند:

اگر کاربر API شخصی تنظیم کرده باشد → استفاده از API شخصی

اگر API شخصی نباشد و Shared فعال باشد → استفاده از Shared

اگر هیچ‌کدام نبود → سرویس غیرفعال است