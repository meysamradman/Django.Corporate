# راهنمای رنگ‌بندی استاندارد

این فایل شامل لیست کامل رنگ‌های استاندارد برای استفاده در جداول و Badge ها است.

## 🎨 رنگ‌های وضعیت

| وضعیت | رنگ | استفاده |
|-------|-----|---------|
| **فعال** (is_active: true) | 🟢 `green` | همه جداول (ادمین، کاربر، نمونه‌کار، دسته‌بندی، تگ، گزینه) |
| **غیرفعال** (is_active: false) | 🔴 `red` | همه جداول |
| **منتشر شده** (status: "published") | 🟢 `green` | نمونه‌کارها |
| **پیش‌نویس** (status: "draft") | 🟡 `yellow` | نمونه‌کارها |

## 📦 دسته‌بندی‌ها

| نوع | رنگ | استفاده |
|-----|-----|---------|
| **دسته‌بندی** (Categories) | 🟣 `purple` | جداول دسته‌بندی نمونه‌کارها |
| **تگ‌ها** (Tags) | 🔵 `indigo` | جداول تگ‌های نمونه‌کارها |
| **گزینه‌ها** (Options) | 🔷 `teal` | جداول گزینه‌های نمونه‌کارها |

**نکته:** برای CardWithIcon در اطلاعات نمونه‌کار، رنگ‌های icon و background هم باید مطابق با این رنگ‌ها باشند:
- **دسته‌بندی**: `iconBgColor="bg-purple"`, `iconColor="stroke-purple-2"`, `borderColor="border-b-purple-1"`
- **تگ**: `iconBgColor="bg-indigo"`, `iconColor="stroke-indigo-2"`, `borderColor="border-b-indigo-1"`
- **گزینه**: `iconBgColor="bg-teal"`, `iconColor="stroke-teal-2"`, `borderColor="border-b-teal-1"`

## 🌐 عمومی/خصوصی

| وضعیت | رنگ | استفاده |
|-------|-----|---------|
| **عمومی** (is_public: true) | 🔵 `blue` | دسته‌بندی، تگ، گزینه |
| **خصوصی** (is_public: false) | ⚪ `gray` | دسته‌بندی، تگ، گزینه |

## 📧 ایمیل

| وضعیت | رنگ | استفاده |
|-------|-----|---------|
| **جدید** (new) | 🔴 `red` | وضعیت ایمیل‌های جدید |
| **خوانده شده** (read) | 🔵 `blue` | وضعیت ایمیل‌های خوانده شده |
| **پاسخ داده شده** (replied) | 🟢 `green` | وضعیت ایمیل‌های پاسخ داده شده |
| **پیش‌نویس** (draft) | 🟠 `orange` | وضعیت ایمیل‌های پیش‌نویس |
| **آرشیو شده** (archived) | ⚪ `gray` | وضعیت ایمیل‌های آرشیو شده |
| **ستاره‌دار** (starred) | 🟡 `amber` | ایمیل‌های ستاره‌دار |
| **هرزنامه** (spam) | 🔴 `red` | ایمیل‌های هرزنامه |
| **سطل زباله** (trash) | ⚪ `gray` | ایمیل‌های حذف شده |
| **ارسال شده** (sent) | 🟢 `green` | ایمیل‌های ارسال شده |
| **صندوق ورودی** (inbox) | 🔵 `blue` | صندوق ورودی (Primary) |

## 🎬 مدیا

| نوع | رنگ | استفاده |
|-----|-----|---------|
| **تصویر** (image) | 🔵 `blue` | نمایش نوع مدیا |
| **ویدیو** (video) | 🟣 `purple` | نمایش نوع مدیا (در OverviewTab و گالری) |
| **صدا** (audio) | 🌸 `pink` | نمایش نوع مدیا (در OverviewTab و گالری) |
| **سند** (document/pdf) | ⚪ `gray` | نمایش نوع مدیا (در OverviewTab) |

**نکته:** در کامپوننت‌های MediaPreview و MediaThumbnail از رنگ‌های متفاوتی استفاده می‌شود:
- **ویدیو**: `bg-red/10` یا `bg-red/20` (قرمز روشن)
- **صدا**: `bg-blue/10` یا `bg-blue/20` (آبی روشن)
- **سند**: `bg-orange/10` یا `bg-orange/20` (نارنجی روشن)

## ⚙️ تنظیمات

| نوع | رنگ | استفاده |
|-----|-----|---------|
| **تنظیمات پسورد / Security** | 🟡 `amber` | تب Security در پروفایل |
| **تنظیمات پیشرفته** (Advanced) | 🟣 `purple` | تب Advanced Settings |
| **تنظیمات معمولی** (General) | 🔵 `blue` | تنظیمات عمومی پنل |
| **سئو** (SEO) | 🟢 `emerald` | تب SEO در نمونه‌کارها |
| **برچسب‌های Meta** | 🟢 `emerald` | بخش Meta Tags در تب SEO |
| **پیش‌نمایش Open Graph** | 🔵 `blue` | بخش Open Graph در تب SEO |
| **تصویر Open Graph** | 🔵 `blue` | تصویر Open Graph در تب SEO |

## ⭐ ویژه بودن

| وضعیت | رنگ | استفاده |
|-------|-----|---------|
| **ویژه** (is_featured: true) | 🟠 `orange` | نمونه‌کارها |
| **عادی** (is_featured: false) | ⚪ `gray` | نمونه‌کارها |

## 👤 نقش‌ها و ادمین‌ها

| نوع | رنگ | استفاده |
|-----|-----|---------|
| **نقش‌ها** (Roles) | 🔵 `blue` | رنگ پیش‌فرض (از ROLE_COLORS استفاده می‌کند) |
| **سوپر ادمین** | 🔵 `default` | Badge variant: default (blue/primary) |
| **ادمین عادی** | ⚪ `outline` | Badge variant: outline |
| **کل ادمین‌ها** (Statistics) | 🟢 `emerald` | کارت آمار ادمین‌ها در داشبورد |
| **کل کاربران** (Statistics) | 🔵 `sky` | کارت آمار کاربران در داشبورد |
| **اطلاعات احراز هویت** (BaseInfoTab) | 🔵 `primary` | بخش اطلاعات احراز هویت در فرم ایجاد/ویرایش ادمین و کاربر |
| **اطلاعات شخصی** (ProfileTab) | 🔵 `blue` | بخش اطلاعات شخصی در پروفایل ادمین و کاربر |
| **موقعیت جغرافیایی** (ProfileTab) | 🟢 `green` | بخش موقعیت جغرافیایی در پروفایل ادمین و کاربر |

## 🤖 هوش مصنوعی

| نوع | رنگ | استفاده |
|-----|-----|---------|
| **هوش مصنوعی** (AI) | 🌸 `pink` | بخش‌های مربوط به AI |

## 📝 فرم‌ها

| وضعیت | رنگ | استفاده |
|-------|-----|---------|
| **فیلد فعال** (is_active: true) | 🔵 `blue` | فیلدهای فعال در فرم‌ها |
| **فیلد غیرفعال** (is_active: false) | ⚪ `gray` | فیلدهای غیرفعال در فرم‌ها |
| **فیلد الزامی** (required) | 🔴 `red` | فیلدهای الزامی در فرم‌ها |

## 🔵 پیش‌فرض

| نوع | رنگ | استفاده |
|-----|-----|---------|
| **پیش‌فرض** (Default) | 🔵 `blue` | برای المان‌هایی که رنگ خاصی ندارند (Primary color) |

---

## 📝 نحوه استفاده

### Badge ها

```tsx
// مثال: فعال/غیرفعال
<Badge variant={isActive ? "green" : "red"}>
  {isActive ? "فعال" : "غیرفعال"}
</Badge>

// مثال: منتشر شده/پیش‌نویس
<Badge variant={status === "published" ? "green" : "yellow"}>
  {status === "published" ? "منتشر شده" : "پیش‌نویس"}
</Badge>

// مثال: عمومی/خصوصی
<Badge variant={isPublic ? "blue" : "gray"}>
  {isPublic ? "عمومی" : "خصوصی"}
</Badge>

// مثال: دسته‌بندی، تگ، گزینه
<Badge variant="purple">دسته‌بندی</Badge>
<Badge variant="indigo">تگ</Badge>
<Badge variant="teal">گزینه</Badge>
```

### CardWithIcon (برای اطلاعات نمونه‌کار)

```tsx
// دسته‌بندی
<CardWithIcon
  icon={FolderOpen}
  title="دسته‌بندی‌ها"
  iconBgColor="bg-purple"
  iconColor="stroke-purple-2"
  borderColor="border-b-purple-1"
  titleExtra={<Badge variant="purple">{count} مورد</Badge>}
>
  {/* محتوا */}
</CardWithIcon>

// تگ
<CardWithIcon
  icon={Tag}
  title="تگ‌ها"
  iconBgColor="bg-indigo"
  iconColor="stroke-indigo-2"
  borderColor="border-b-indigo-1"
  titleExtra={<Badge variant="indigo">{count} مورد</Badge>}
>
  {/* محتوا */}
</CardWithIcon>

// گزینه
<CardWithIcon
  icon={FileCode}
  title="گزینه‌ها"
  iconBgColor="bg-teal"
  iconColor="stroke-teal-2"
  borderColor="border-b-teal-1"
  titleExtra={<Badge variant="teal">{count} مورد</Badge>}
>
  {/* محتوا */}
</CardWithIcon>
```

## ✅ چک‌لیست اعمال رنگ‌ها

- [x] جداول ادمین‌ها (AdminTableColumns.tsx)
- [x] جداول کاربران (UserTableColumns.tsx)
- [x] جداول نمونه‌کارها (PortfolioTableColumns.tsx)
- [x] جداول دسته‌بندی‌ها (CategoryTableColumns.tsx)
- [x] جداول تگ‌ها (TagTableColumns.tsx)
- [x] جداول گزینه‌ها (OptionTableColumns.tsx)
- [x] جداول نقش‌ها (RoleTableColumns.tsx)
- [x] Sidebar نمونه‌کار (PortfolioSidebar.tsx)
- [x] اطلاعات نمونه‌کار - OverviewTab (OverviewTab.tsx) - شامل CardWithIcon و Badge ها
- [x] بخش‌های ایمیل (EmailItem.tsx, EmailDetailView.tsx, EmailSidebar.tsx)
- [x] فرم‌ها (FormFieldsSection.tsx)
- [x] مدیا (MediaPreview.tsx, MediaThumbnail.tsx)

**نکته:** `variant="outline"` و `variant="default"` variant های استاندارد Badge هستند و برای موارد خنثی یا پیش‌فرض استفاده می‌شوند.

