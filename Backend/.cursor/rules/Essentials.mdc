---
alwaysApply: true
---

django +5.2.6  بک اند رو میسازم البته با Django REST framework 
 و با next js 15.5.3 پنل ادمین رو میسازم و نمیخوام از پنل ادمین دیجنگو استفاده کنم.
من از api استفاده میکنم.
 Core داریم که موارد زیر باید ازش استفاده بشه :

تقریبا باید همهجا استفاده بشه از مدل base.py به جز اپ یوزر که تداخل نداشته باشیم و  برای استفاده اپ ها که راحتر باشه و مشکل ایجاد نکنه.
همهجا باید همیشه از pagination.py استفاده بشه.
همهجا باید همیشه از response.py استفاده بشه.





