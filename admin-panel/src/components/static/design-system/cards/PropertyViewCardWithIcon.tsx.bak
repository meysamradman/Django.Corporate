import { Info } from "lucide-react";
import { CardWithIcon } from "@/components/elements/CardWithIcon";

export function PropertyViewCardWithIcon() {
  return (
    <CardWithIcon
      icon={Info}
      title="جزئیات و مشخصات ملک"
      iconBgColor="bg-blue"
      iconColor="stroke-blue-2"
      cardBorderColor="border-b-blue-1"
      className="shadow-sm"
      contentClassName=""
    >
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4 text-font-p">
        <div className="rounded-lg border border-br bg-bg p-3">
          <p className="text-xs text-font-s">بخش اول</p>
          <p className="mt-1 text-sm font-semibold">ابعاد و فیزیکی</p>
        </div>
        <div className="rounded-lg border border-br bg-bg p-3">
          <p className="text-xs text-font-s">بخش دوم</p>
          <p className="mt-1 text-sm font-semibold">امکانات و فضاها</p>
        </div>
        <div className="rounded-lg border border-br bg-bg p-3">
          <p className="text-xs text-font-s">بخش سوم</p>
          <p className="mt-1 text-sm font-semibold">اطلاعات مالی</p>
        </div>
      </div>
    </CardWithIcon>
  );
}
