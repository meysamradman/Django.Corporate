ببین یه چیز که خیلی داره ادیتم میکنه اینه استایل و کدهای فرانت پنل ادمینه ما از المنتها با tailwind استفاده میکنیم 4.1 حالا خیلی جاها استایل اضافی داره و یه سری صفحات اصلا دیزاین درست ندارن و یه سری صفحات دیزاینشون باید یکی شه حالا نمیدونم برای این موضوع چیکار کنم ایا اول برای صفحات بیام جدا استاتیک درست کنم در جای دیگه و بعد از همونا مپی بگیریم برای صفحات و یا نه راه دیگری هست تو چی پیشنهاد میدی چی کار کنیم؟
نه منظورم این بود استاتیک در همین پروژ] درست شه برای صفحات بعد که استایل مطمان شدم مپی بگیریم استایل برای صفحات و موردی که گفتی بیایم برای صفحاتLayout Components درست کنیم سرعتو برای پنل کم میکنه البته برای خیلی فرمها جداول اینکارو کردیم تو هنوز درست فایلهارو ندیدی حالا نمیدون سرچ کن اینترنت بهترین راه چیه؟
اینی که میگی خوبه اما چون المنت داریم برای مثلا دکمه اینا که مشخصه در اصل منظورم طراحی صفحات مثلا ایجاد صفحاتش بیشتر جاها یکی و مثلا مشاهده املاک وبلاگ و نمونهکار یکیه بیشتر جرئیات فرق داره متوحه میشی هر کدامم هم کامپوننت دارند داخلشون و نامهاشون استایلشون میدونی اینا باید درست باشه متوجه شدی؟
ببین سرعت کم میشه و اینکه شاید مثلا بخوام استایل املاک فرق کنه میدونی اینا مهمه شاید همهچی شبیهخ هم نباشه فکر میکنم بهترین راه همون استاتیکه مثلا دو جور صفحه ایجاد و مثلا سایبار و نقشه صفحات ایحاد میدونی اینا رو میگم
برای پنل ادمین react vite با shadcn البته المنتشو فقط برداشتم سرچ کن و نظرتو بگو

# 📘 مستند معماری طراحی صفحات پنل ادمین (Page Pattern Approach)

## 🎯 هدف این داکیومنت

این سند برای حل مشکلات زیر نوشته شده است:

* ناهماهنگی طراحی صفحات پنل ادمین
* تکرار زیاد کد و استایل
* سختی نگهداری و توسعه در بلندمدت
* نگرانی درباره Performance در React

هدف نهایی:

> **ایجاد یک ساختار حرفه‌ای، سریع، قابل نگهداری و قابل توسعه برای طراحی صفحات پنل ادمین**

---

## 🧠 تصمیم معماری نهایی (Final Decision)

### ✅ رویکرد انتخاب‌شده

**Page Pattern / Layout Components داخل خود پروژه**

### ❌ رویکردهای ردشده

* ساخت صفحات Static جداگانه و mapping بعدی
* استفاده از Admin Templateهای آماده
* کپی‌پیست طراحی در هر صفحه
* Generic Layoutهای بیش‌ازحد پیچیده

---

## 🧩 تعریف Page Pattern چیست؟

Page Pattern یک کامپوننت React است که:

* فقط مسئول **ساختار صفحه (Structure)** است
* هیچ منطق بیزینسی ندارد
* هیچ data fetching ندارد
* هیچ permission check ندارد

Page Pattern مانند یک **فریم ثابت** عمل می‌کند که محتوا داخل آن قرار می‌گیرد.

---

## 🧱 انواع Page Pattern در پنل ادمین

در پنل ادمین ما فقط به چند الگوی اصلی نیاز داریم:

### 1️⃣ Create / Edit Page Pattern

مخصوص صفحات ایجاد و ویرایش (املاک، وبلاگ، نمونه‌کار و ...)

ساختار کلی:

* Header (عنوان + اکشن‌ها)
* Content Area (فرم اصلی)
* Sidebar (اختیاری – مثل نقشه)
* Footer Actions (ذخیره، لغو)

---

### 2️⃣ Detail / View Page Pattern

مخصوص صفحات مشاهده جزئیات

ساختار کلی:

* Header (عنوان + اکشن‌ها)
* Main Content
* Sections / Tabs
* Sidebar اختیاری

---

### 3️⃣ List Page Pattern

مخصوص صفحات لیست

ساختار کلی:

* Header + Actions
* Filters
* Table / Cards

---

## 📁 ساختار فایل پیشنهادی

```
src/
 ├─ components/
 │   ├─ ui/               // Button, Input, Table (Element-level)
 │   ├─ page-patterns/    // CreatePageLayout, DetailPageLayout, ListPageLayout
 │   └─ modules/          // PropertyForm, BlogForm, ...
 │
 └─ pages/                // Route-level pages
```

---

## 🟢 قوانین طلایی Page Pattern (خیلی مهم)

### ✅ مجاز داخل Page Pattern

* grid / flex
* spacing
* layout structure
* slots (children, sidebar, headerActions)
* variantهای ظاهری (مثلاً property / blog)

### ❌ غیرمجاز داخل Page Pattern

* data fetching
* business logic
* validation
* permission checks
* stateهای پیچیده

> **Layout = Structure فقط**

---

## ⚡ Performance – چرا این روش کند نیست؟

طبق مستندات رسمی React و ReactPatterns:

* Layout Components معمولاً Stateless هستند
* Re-render بسیار کمی دارند
* در صورت نیاز می‌توانند memoized شوند

نتیجه:

> Page Pattern مانند قاب ثابت عمل می‌کند و محتوای داخل آن آزادانه آپدیت می‌شود

این دقیقاً همان رویکردی است که در:

* Strapi Admin
* Refine
* AdminJS
* Directus

استفاده شده است.

---

## 🎨 تفاوت طراحی بین ماژول‌ها (املاک، وبلاگ، ...)

تفاوت‌ها از طریق موارد زیر اعمال می‌شوند:

* variant
* props اختیاری (sidebar، map، extra sections)
* کلاس‌های اختصاصی محدود

بدون:

* fork کردن کل Layout
* کپی‌پیست صفحه

---

## 🔁 فرآیند مهاجرت صفحات (Migration Plan)

### فاز 1 (این هفته)

* ساخت CreatePageLayout و DetailPageLayout
* مهاجرت 2 تا 3 صفحه واقعی

### فاز 2

* بررسی UX و Performance
* اصلاح spacing و structure

### فاز 3

* تعمیم به سایر صفحات
* حذف کدهای تکراری قدیمی

---

## ❌ Anti-Patterns (چیزهایی که نباید انجام شوند)

* ساخت Layoutهای بیش‌ازحد Generic
* اضافه‌کردن fetch داخل Layout
* شرط‌های بیزینسی داخل Layout
* ساخت Static Page جدا از پروژه

---

## ✅ مزایای نهایی این معماری

* 🎯 طراحی یکدست صفحات
* ⚡ Performance بالا
* 🧠 خوانایی و نگهداری آسان
* 🔁 توسعه سریع‌تر صفحات جدید
* 🔒 کاهش باگ‌های UI

---

## 🏁 جمع‌بندی نهایی

**Page Pattern داخل پروژه، بهترین و حرفه‌ای‌ترین راه برای طراحی پنل ادمین است.**

این رویکرد:

* مورد تأیید منابع رسمی React
* استفاده‌شده در سیستم‌های بزرگ
* مناسب تیم‌های در حال رشد

و دقیقاً متناسب با نیاز پروژه فعلی شماست.

---

📌 این داکیومنت مرجع رسمی تصمیم معماری UI پنل ادمین محسوب می‌شود.
